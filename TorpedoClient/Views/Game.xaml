<Page x:Class="TorpedoClient.Views.Game"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TorpedoClient.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Game"
      Background="Gray">
    <DockPanel>
        <Label Style="{StaticResource Header}" Content="Állapot text" DockPanel.Dock="Top"/>

        <Grid DockPanel.Dock="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <Rectangle Fill="Orange" Grid.RowSpan="5" Width="50"/>
        </Grid>

        <Grid DockPanel.Dock="Right">
            <Label Style="{StaticResource Header}" Content="Ide jön a grid"/>
        </Grid>

        <Grid DockPanel.Dock="Bottom">
            <Label Style="{StaticResource Header}" Content="Gombok"/>
        </Grid>
    </DockPanel>
</Page>
